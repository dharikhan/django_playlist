---
- name: Installing and Running django Framework
  hosts: localhost

  tasks:
        - name: install django
          pip: state:latest
              name: httpd
              state: present

        - name: started httpd
          service: 
              name: httpd
              state: started